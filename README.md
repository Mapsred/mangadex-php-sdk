# Mangadex PHP SDK (auto-generated and adapted)

```bash
docker run --rm -v ${PWD}:/local openapitools/openapi-generator-cli generate \
  -i https://api.mangadex.org/api.yaml \
  -g php -o /local/out/php
  ```
